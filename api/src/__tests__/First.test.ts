describe("First", () => {
  it("deve ser posivel somar dois numeros", () => {
    expect(2 + 2).toBe(4);
  });

  it("deve ser posivel somar dois numeros", () => {
    expect(2 + 2).not.toBe(5);
  });
});
